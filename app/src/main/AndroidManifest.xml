<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mscisz.damian.calculator">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnterBasicDataActivity" />
        <activity android:name=".ActivityAddProduct" />
        <activity
            android:name=".ActivityShowMealByDate"
            android:noHistory="true" />
        <activity android:name=".ActivityAddMeal" />
        <activity android:name=".ActivityAddSports" />
        <activity android:name=".ActivityPracticedSports" />
        <activity android:name=".ActivityProgress" />
        <activity android:name=".Repeating_activity" />

        <receiver android:name=".Notification_receiver" />

        <activity android:name=".ActivityEditUserData"></activity>
    </application>

</manifest>